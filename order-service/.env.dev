# Dev environment overrides. Values here take precedence over .env when APP_ENV=dev.
# When running via Docker Compose the defaults in .env are fine — no changes needed here.
# If running locally (outside Docker), uncomment and adjust:

#APP_SECRET=change_me
#DATABASE_URL="mysql://app:app@127.0.0.1:3306/order_db?serverVersion=8.0&charset=utf8mb4"
#MESSENGER_TRANSPORT_DSN=amqp://guest:guest@127.0.0.1:5672/%2f/messages
